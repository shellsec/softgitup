# 本地服务器使用说明

## 功能说明

现在 SoftGitUp 支持从本地 Web 服务器或文件服务器下载文件进行更新。这对于以下场景特别有用：

1. **内网环境**：在没有互联网访问的环境中，可以在内网搭建文件服务器
2. **快速更新**：本地服务器通常比远程服务器更快
3. **离线更新**：完全离线环境下的软件更新

## 使用方法

### 1. 启动本地服务器

有两种方式启动本地服务器：

#### 方式一：使用批处理文件（推荐）

双击运行 `启动本地服务器.bat`，默认在 8000 端口启动服务器。

如果需要使用其他端口，可以在命令行中运行：
```batch
启动本地服务器.bat 8080
```

#### 方式二：使用 Python 脚本

```bash
python 启动本地服务器.py
```

或指定端口：
```bash
python 启动本地服务器.py 8080
```

### 2. 配置本地服务器地址

在 `config.json` 文件中，设置 `git_platform` 为 `"remote"`，并配置 `remote_server` 字段：

```json
{
    "git_platform": "remote",
    "remote_server": "http://your-server-url:8000",
    ...
}
```

**支持的地址格式：**
- `http://localhost:8000` - 本地服务器
- `http://127.0.0.1:8000` - 本地服务器（IP形式）
- `http://192.168.1.100:8000` - 局域网服务器
- `http://10.0.0.1:8000` - 内网服务器
- `http://example.com:8000` - 任意 URL 地址的服务器
- `https://your-domain.com/software` - HTTPS 服务器

### 3. 服务器目录结构

本地服务器会以项目根目录为根目录提供服务。确保目录结构如下：

```
项目根目录/
├── software/
│   ├── list.txt          # 软件列表文件
│   ├── 软件名1/
│   │   └── 文件...
│   └── 软件名2/
│       └── 文件...
├── config.json
└── ...
```

### 4. 访问测试

启动服务器后，可以通过浏览器访问：

- 根目录：`http://localhost:8000/`
- 软件列表：`http://localhost:8000/software/list.txt`
- 软件文件：`http://localhost:8000/software/软件名/文件路径`

### 5. 使用同步功能

配置好 `git_platform` 和 `remote_server` 后，运行同步脚本：

- `simple_sync.py` - Python 同步脚本
- `一键快速同步.bat` - 批处理同步脚本
- `soft_sync.py` - 完整同步工具

**重要说明：**
- 当 `git_platform` 设置为 `"remote"` 时，系统**只使用** `remote_server` 配置的服务器
- 如果 `git_platform` 是 `"github"` 或 `"gitlab"`，系统会使用相应的远程仓库
- `remote` 模式不支持自动回退到其他源，确保服务器可用

## 注意事项

1. **git_platform 设置**：使用远程服务器时，必须将 `git_platform` 设置为 `"remote"`
2. **remote_server 配置**：当 `git_platform` 为 `"remote"` 时，必须配置 `remote_server`，否则会报错
3. **防火墙设置**：如果使用局域网地址，确保防火墙允许相应端口的访问
4. **端口占用**：如果默认端口 8000 被占用，请使用其他端口
5. **路径格式**：本地服务器使用标准 HTTP 路径，不需要分支信息
6. **URL 格式**：支持任意 HTTP/HTTPS URL，不限于 localhost 或内网 IP
7. **无自动回退**：`remote` 模式不会自动回退到其他源，确保服务器可用

## 示例配置

### 示例 1：使用本地服务器（remote 模式）

```json
{
    "git_platform": "remote",
    "remote_server": "http://localhost:8000"
}
```

### 示例 2：使用局域网服务器

```json
{
    "git_platform": "remote",
    "remote_server": "http://192.168.1.100:8080"
}
```

### 示例 3：使用任意 URL 的服务器

```json
{
    "git_platform": "remote",
    "remote_server": "http://example.com:8000"
}
```

### 示例 4：使用 GitHub（默认模式）

```json
{
    "git_platform": "github",
    "github_repo": "https://github.com/shellsec/softgitup",
    "git_mirrors": [
        "https://gh-proxy.com/https://raw.githubusercontent.com/shellsec/softgitup"
    ]
}
```

### 示例 5：使用 GitLab

```json
{
    "git_platform": "gitlab",
    "gitlab_repo": "https://gitlab.com/your-username/softgitup"
}
```

## 故障排除

### 问题 1：无法连接到本地服务器

**解决方案：**
- 检查服务器是否已启动
- 检查端口是否正确
- 检查防火墙设置
- 尝试使用 `127.0.0.1` 代替 `localhost`

### 问题 2：文件下载失败

**解决方案：**
- 检查文件路径是否正确
- 检查服务器目录结构是否匹配
- 查看服务器日志输出

### 问题 3：端口已被占用

**解决方案：**
- 使用其他端口：`启动本地服务器.bat 8080`
- 或关闭占用端口的程序

## 技术说明

本地服务器使用 Python 的 `http.server` 模块，支持：
- 标准 HTTP GET 请求
- 文件下载
- CORS 跨域支持（用于 Web 应用）
- 目录浏览

如果需要更高级的功能（如认证、HTTPS 等），建议使用专业的 Web 服务器（如 Nginx、Apache）或 Python Web 框架（如 Flask、FastAPI）。

